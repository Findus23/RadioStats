import Vuex from 'vuex';
import Vue from 'vue';

Vue.use(Vuex);

const state = {
    songs: []
};

const mutations = {};

const actions = {};

export default new Vuex.Store({
    state: state,
    mutations: mutations,
    actions: actions
});
